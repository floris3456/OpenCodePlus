# Retained deliberately: round-3 T3 integration patch marker

This path is a terminal evidence tombstone for the applied T3 replay. It is
not a pending patch and it is not awaiting removal.

- The patch content was replayed in full onto the integrated sources; no patch
  remains to apply.
- The applied implementation lives in `packages/plus/src/index.ts` (guards and
  error declared in `packages/plus/src/rpc.ts`).
- The evidence record, guard map and check receipts live in
  `packages/plus/docs/round3-t3-evidence.md`.
- The original patch content remains retrievable from git history.

No further action is required.